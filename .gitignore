node_modules/
dist/
*.log
.env
*.sqlite
*.sqlite-journal

# Secrets and local eval scratch — never commit
.openai-api-key
tmp/
eval/out/

# Autotune local run artifacts (generated gold set, profiles, ledger, reports)
# Anchored to repo root so it does NOT match packages/autotune/ (the source).
/autotune/

# Isolated worktrees
.worktrees/
